// This is the code for the finder interface.
// Simpley make bolt an accessible function, that runs, and returns a path
var sk = require('SimpleKeys');

var bolt = module.exports = function(callback){
	callback('/Users/njbleaux/Repos');
};
